% Nagare system architecture (article Fig. 1). Standalone-compilable: pdflatex fig1-architecture.tex
\documentclass[tikz,border=8pt]{standalone}
\usetikzlibrary{positioning,fit,backgrounds}
\begin{document}
\begin{tikzpicture}[
  font=\footnotesize,
  box/.style={draw,rounded corners,align=center,minimum height=0.9cm,inner sep=3pt,fill=white},
  app/.style={box,fill=teal!12},
  lrn/.style={box,fill=orange!14},
  op/.style={box,fill=blue!8,text width=3.0cm},
  disc/.style={draw,rounded corners,align=center,fill=gray!12,minimum height=1.0cm,text width=15.5cm},
  band/.style={font=\bfseries\footnotesize,align=center},
  arr/.style={-{Latex[length=2mm]},gray,thick},
]
  % ---- Applications
  \node[app,text width=2.9cm] (nc)  at (0,7)   {Neocognitron\\recognition + pose};
  \node[app,text width=2.9cm] (det) at (3.7,7) {SBSH detector\\quadtree grid};
  \node[app,text width=2.9cm] (sl)  at (7.4,7) {Signed-link\\leakage audit};
  \node[app,text width=2.9cm] (ev)  at (11.1,7){Evolvent\\online learning};
  \node[band] at (-2.3,7) {Applications};

  % ---- Learners
  \node[lrn,text width=6.3cm] (bp)  at (1.85,5.1) {Backprop: closed-form gradient + Adam\\ $O(d)$/step, iterative (sample-slow)};
  \node[lrn,text width=6.3cm] (rls) at (9.25,5.1) {EvolventHead: forgetting-RLS\\ $O(d^2)$/step, one-pass (sample-fast)};
  \node[band] at (-2.3,5.1) {Learners};

  % ---- Op library
  \node[op] (o1) at (0,3)    {Core\\linear $\cdot$ mse $\cdot$ loss $\cdot$ adam};
  \node[op] (o2) at (3.7,3)  {Function approx\\kan $\cdot$ hsikan $\cdot$ chebyshev-CR};
  \node[op] (o3) at (7.4,3)  {Geometry / rotors\\dihedral $\cdot$ cayley\_rotor $\cdot$ rotor\_holonomy $\cdot$ rotor\_spike};
  \node[op] (o4) at (11.1,3) {Graph / hypergraph\\hg\_message $\cdot$ signed\_scatter $\cdot$ gomb\_shell};
  \node[op] (o5) at (1.85,1.4) {Entropy / pooling\\global\_entropy\_pool $\cdot$ spectral\_entropy $\cdot$ phase\_pool};
  \node[op] (o6) at (9.25,1.4) {CV\\conv2d $\cdot$ group\_pool $\cdot$ sc\_block $\cdot$ soft\_argmax $\cdot$ gaussian\_kld};
  \node[band,text width=1.9cm] at (-2.3,2.2) {Closed-form op library\\(31 FD-verified)};

  % ---- Discipline
  \node[disc] (disc) at (5.55,-0.2) {Discipline: closed-form $\cdot$ hand-derived backward $\cdot$ FD-verified $\cdot$ \textbf{no autograd}\\ + Assimilation lifecycle (component registry + findings ledger)};

  \draw[arr] (bp.north)  -- ++(0,0.5);
  \draw[arr] (rls.north) -- ++(0,0.5);
  \draw[arr] (5.55,4.3) -- (5.55,3.9);
  \draw[arr] (5.55,0.55) -- (5.55,0.35);
\end{tikzpicture}
\end{document}
